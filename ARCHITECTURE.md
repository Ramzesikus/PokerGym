# PokerGym — ARCHITECTURE.md

**Версия: v0**

Это стартовая версия документа — точка отсчёта перед началом реального разбиения кода. Нумерация версий идёт от неё: первая содержательная правка после начала работы над Этапом 1 станет v1, и далее по нарастающей. Версия указывается и в самом файле, и в имени файла (`ARCHITECTURE_v0.md`, далее `ARCHITECTURE_v1.md` и т.д. — по аналогии с версионированием `poker-trainer_v15_X.html`), чтобы в Документах проекта было видно историю по именам файлов, а не только по содержимому.

Это живой документ. Обновляется по ходу разработки, не разовый снимок. Версия увеличивается на 1 при каждой содержательной актуализации файла (не при мелких точечных правках вроде переименования одного ID).

**Важная дисциплина:** таблица в разделе 1 всегда отражает *текущее* состояние репозитория, а не план. Всё, что ещё не написано — в разделе 3 (План игры / Backlog). Когда часть работы запушена на GitHub — таблицу нужно попросить обновить.

Источник для таблицы: `poker-trainer_v15_35.html` (7012 строк, один файл, ещё не разбит — разбиение только спланировано).

Содержание:
1. [Таблица компонентов](#1-таблица-компонентов)
2. [DECISIONS — журнал решений](#2-decisions--журнал-решений)
3. [План игры](#3-план-игры)

---

## 1. Таблица компонентов

**Как читать столбцы:**

| Столбец | Смысл |
|---|---|
| ID | Короткий код, отражает целевой путь файла |
| Кусок / модуль | Человеческое название |
| Строк сейчас | Диапазон строк в текущем `poker-trainer_v15_35.html` |
| Путь в новой структуре | Куда переносится при разбиении (план, согласован, не выполнен) |
| Технология | HTML/CSS/JS — JSON появится вместе с `localStorage` и `manifest.json` |
| Что делает | Подробное описание |
| Где используется | Вкладка / тренировка / функция |
| Статус | готово / заглушка / не начато |
| Выполняется сейчас | клиент / сервер |
| Кандидат на сервер | да / нет / частично |
| Хостинг | статика / сервер API / БД — на будущее |
| Качается на устройство | одинаково для телефона (PWA) и компьютера — нет разделения бандла по платформам |
| Примечания | зависимости, риски, найденные при анализе факты |

| ID | Кусок / модуль | Строк сейчас | Путь в новой структуре (план) | Технология | Что делает | Где используется | Статус | Выполняется сейчас | Кандидат на сервер | Хостинг | Качается на устройство | Примечания |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CSS-STYLES | Все стили | 7–1775 | `css/styles.css` | CSS | Визуальная стилистика: экраны, карточки, кнопки, темы, модалки | Везде | Готово | Клиент | Нет | Статика | Да, целиком | — |
| HTML-SHELL | Разметка экранов и модалок | 1776–3001 | `index.html` (шаблон) | HTML | Статичная разметка: шапка, таб-бар, экраны настроек и теории, интро-модалка | Везде | Готово | Клиент | Нет | Статика | Да, целиком | Часть уйдёт в JS-шаблоны при разборе |
| LIB-SORTABLE | SortableJS 1.15.7 | 3002–3009 (1 строка) | `js/lib/sortable.js` | JS (сторонняя, ESM-сборка) | Drag-and-drop карточек в хабе «Практика» | `practice.js`, режим редактирования | Готово | Клиент | Нет | Статика | Да, целиком | **Решение изменено:** брать официальную ESM-сборку (`sortable.esm.js` из пакета/CDN), не UMD — тогда `import Sortable from '../lib/sortable.js'` работает без танцев с глобальной переменной |
| CORE-I18N | Словарь локализации RU/EN | 3011–3814 | `js/core/i18n.js` | JS | Все текстовые строки интерфейса | Везде | Готово | Клиент | Нет | Статика | Да, целиком | Есть «осиротевшие» ключи без модуля в `HUB_MODULES` (`module.identifyCategories.title`, `module.handprob.title`) — уточнить при разборе |
| CORE-STATE | Глобальное состояние и константы | 3816–3849 | `js/core/state.js` | JS | Объект `state`, цвета тем, константы карт | Везде | Готово | Клиент | Нет | Статика | Да, целиком | **Правило:** должен оставаться чистым хранилищем данных, не импортировать ничего из UI/фич-модулей — иначе риск циклических зависимостей. Другие модули сами подписываются на изменения (простой Pub/Sub), а не `state` лезет в них |
| CORE-EVAL | Движок оценки руки и комбинаторика | 3850–4098 | `js/core/hand-eval.js` | JS | `evaluate5`, `evaluate7` (универсальна для 5/6/7+ карт — не только семи, при переносе стоит переименовать в `evaluateBest`), `getCoreCards` (карты, формирующие «ядро» комбинации — пара/сет/каре внутри лучших 5 карт), `displayCategoryIndex` (техническая категория → отображаемая, включая отдельный флеш-рояль), `classifyCategories`, `computeOuts`, `buildDeck`, комбинаторика | Тренировки «Ауты», «Комбинации», калькулятор аутов, калькулятор эквити (и его воркер) | Готово | Клиент | Да (стратегия защиты) | Статика (сейчас) | Да, целиком | **Сверено с pokersolver — не менять без веской причины.** Чистая функция, без DOM. `buildDeck` уже требуется самим движком (`computeOuts` его вызывает) — не опционально. `getCoreCards` уже используется «Комбинациями», `displayCategoryIndex` — «Аутами», «Комбинациями» и калькулятором эквити. Обе функции **дублируются целиком** внутри строки воркера — см. `CORE-EQWORKER` |
| PR-TR-OUTS | Тренировка «Ауты и вероятность» | 4099–4557 **+ 6225–6267** | `js/practice/trainings/outs-logic.js` + `outs-ui.js` | JS | Генерация раздачи, таймер показа карт, приём и разбор ответа, включение/выключение кнопки «начать» и переключателя «по категориям» на сетапе | Вкладка «Практика» → «Ауты и вероятность» | Готово | Клиент | Частично | Статика (сейчас) | Да, целиком | `buildDeck`/`shuffle` переносятся в `core/deck.js`. `createCategoryChip` — кандидат на общий `ui/answer-options.js` (см. ниже). Строки 6225–6267 (`updateStartButtonState`, `updatePercategoryAvailability`) физически лежали в диапазоне, который раньше числился общими настройками — на деле это setup-логика именно этой тренировки |
| PR-ROUTER | Навигация по под-экранам «Практики» | 4584–4605 | `js/practice/practice-router.js` | JS | `showSubview` — переключение setup/session-экранов внутри «Практики» | Вкладка «Практика», все тренировки | Готово | Клиент | Нет | Статика | Да, целиком | Должен и инициализировать код открываемого модуля, и давать параметризуемую точку входа («запусти сессию с такими настройками») для программного вызова из Обучариума — не только для клика в хабе |
| ROUTER | Переключение вкладок + кнопка «назад» | 4560–4581, 4869–4881 | `js/router.js` | JS | Клик по нижней навигации, обновление шапки; общая кнопка «назад», которая сейчас напрямую знает детали и «Практики», и «Теории» | Всё приложение | Готово | Клиент | Нет | Статика | Да, целиком | **Ранее не было отдельной строкой** — тихо считалось частью `PR-TR-OUTS` просто по соседству строк, это была ошибка границ, исправлено. Кнопка «назад» — кандидат на будущий рефакторинг: каждая вкладка отдаёт свой `goBack()`, а не общий обработчик знает детали каждой |
| PR-HUB | Хаб «Практика» | 4606–4838 | `js/practice/practice.js` | JS | Список тренировок (`HUB_MODULES`), рендер карточек по группам, закрепление, режим редактирования, drag (через `LIB-SORTABLE`) | Вкладка «Практика», главный экран | Готово | Клиент | Нет | Статика | Да, целиком | Порядок карточек и закрепление живут только в памяти — см. BACKLOG-01 |
| THEORY | Вкладка «Теория» — навигация | 4839–4867 | `js/theory/theory.js` | JS | `showTheorySubview` — переключение под-разделов глоссария (термины/комбинации/категории/нотация) + сброс поиска | Вкладка «Теория» | Готово | Клиент | Нет | Статика | Да, целиком | Это именно роутер вкладки, не сам текст глоссария (он статичен, в `HTML-SHELL`). Переименовано с `THEORY-GLOSSARY` — старое имя вводило в заблуждение. `theory-hub.js` и `sections/` — задел на будущее, когда Теория получит режим редактирования как у Практики, сейчас не существуют |
| PR-TR-NOTATION | Тренировка «Нотация» | 4916–5253, 5494–5560 | `js/practice/trainings/notation-logic.js` + `notation-ui.js` | JS | Три режима (одиночная заявка, множественный выбор, генерация заданий-«токенов»), перевод кода руки в карты и обратно | Вкладка «Практика» → «Нотация» | Готово | Клиент | Нет | Статика | Да, целиком | `notPickRandom` — общая утилита случайного элемента, переносится в `core/deck.js`. Строки 4885–4906 (`NOT_RANKS_FACES`, `NOT_GRID_RANKS`, `notState`) — целиком переезжают сюда же, это собственное состояние именно «Нотации» |
| PR-TR-PREFLOP-GRID | Тренировка «Матрица префлопа» | 5258–5493, 5561–5672 | `js/practice/trainings/preflop-grid-logic.js` + `preflop-grid-ui.js` | JS | Генерация раунда с диапазоном, отрисовка грида 13×13, вопрос на вероятность попадания в диапазон | Вкладка «Практика» → «Матрица префлопа» | Готово | Клиент | Нет | Статика | Да, целиком | **Найдено:** эти функции физически названы с префиксом `not...` и визуально сидят внутри диапазона «Нотации», но по коду роутера (`showSubview`) относятся к отдельному модулю `preflopTable`, не к `notation`. Историческое наследие, не намеренное разделение. `renderNotationRangeGrid` (сама отрисовка грида) — кандидат на выделение в `ui/range-grid.js` отдельно от `checkNotationRange` (проверка ответа остаётся в логике тренировки) — Обучариуму может понадобиться просто показать грид без запуска интерактива. Строки 4907–4915 (`pfState`) — целиком переезжают сюда, это собственное состояние именно «Матрицы префлопа», **проверено по вызовам — с `notState` не пересекается ни разу**, общего файла между ними не нужно |
| PR-TR-POSITIONS | Тренировка «Позиции за столом» | 5673–6029 | `js/practice/trainings/positions-logic.js` + `positions-ui.js` | JS | Генерация раздачи по позициям, подсказки (danger/tail/vector), проверка ответа | Вкладка «Практика» → «Позиции за столом» | Готово | Клиент | Нет | Статика | Да, целиком | `posLabel`/`posTailSeats` → `core/positions.js`. `posShuffle`/`posRandomInt` — дубли, объединяются в `core/deck.js`. `renderPositionsTable` (отрисовка стола с местами, без хинтов/проверки) — кандидат на `ui/table-seats.js`: «стол с местами» — самостоятельный визуальный элемент, вероятно понадобится Обучариуму для сценариев. `renderPositionsOptions` (кнопки-варианты) — см. `ui/answer-options.js` ниже |
| PR-TR-COMBINATIONS | Тренировка «Комбинации» | 6030–6224 | `js/practice/trainings/combinations-logic.js` + `combinations-ui.js` | JS | Генерация руки нужной сложности, проверка выбранной комбинации | Вкладка «Практика» → «Комбинации» | Готово | Клиент | Нет | Статика | Да, целиком | `renderComboOptions` (кнопки-варианты) — см. `ui/answer-options.js` ниже. Использует `getCoreCards`/`displayCategoryIndex` из `CORE-EVAL` |
| SETTINGS | Общие настройки и тема | 6268–6440 | `js/settings/settings.js` | JS | Применение языка, фона стола, авто-темы по системе, превью карт | Вкладка «Настройки», используется везде через `state` | Готово | Клиент | Нет | Статика | Да, целиком | Диапазон сокращён — начало (6225–6267) переехало в `PR-TR-OUTS`, см. примечание там |
| UI-CARDPICKER | Компонент выбора карт | 6441–6539 | `js/ui/card-picker.js` | JS | Универсальный грид выбора карты, последовательный ряд карт | Калькулятор аутов, калькулятор эквити | Готово | Клиент | Нет | Статика | Да, целиком | Уже переиспользуемый компонент, выносится отдельно на первом шаге |
| PR-TR-CALCOUTS | Калькулятор аутов | 6540–6663 | `js/practice/trainings/calc-outs.js` | JS | Ручной ввод руки/борда, расчёт через `CORE-EVAL` | Вкладка «Практика» → «Калькулятор аутов» | Готово | Клиент | Да | Статика (сейчас) | Да, целиком | Ниже порога ~150–200 строк — логика/UI не разделяются |
| PR-TR-CALCEQT | Калькулятор эквити — интерфейс | 6664–6791, 6950–6951 | `js/practice/trainings/calc-equity.js` | JS | UI: до 9 игроков, ввод рук/борда, прогресс-бар, рендер результатов | Вкладка «Практика» → «Калькулятор эквити» | Готово | Клиент | Да | Статика (сейчас) | Да, целиком | Логика уже отдельно в воркере — здесь чистый UI |
| CORE-EQWORKER | Web Worker для расчёта эквити | 6792–6949 (строка-шаблон внутри `PR-TR-CALCEQT`) | `js/core/equity-worker.js` (отдельный файл) | JS (модульный Web Worker) | Монте-Карло расчёт эквити рук друг против друга | Калькулятор эквити | Готово | Клиент | Да | Статика (сейчас) | Да, целиком | **Дублирует внутри себя** `evaluate5`/`evaluate7`/`displayCategoryIndex` из `CORE-EVAL` и перемешивание (третья копия, после `PR-TR-OUTS` и `PR-TR-POSITIONS`). **Решение по ESM:** делать модульным воркером (`new Worker(url, { type: 'module' })`, обычный `import`, путь к `hand-eval.js` через `import.meta.url`, не жёсткий абсолютный путь от корня домена — иначе сломается при деплое в подпапку), не через `importScripts` — единообразно с остальным проектом. Проверить актуальную поддержку в целевых браузерах ближе к реализации |
| APP-BOOT | Bootstrap / точка входа | 6952–7012 | `js/app.js` | JS | Обработчики кнопок эквити, первый рендер, вступительная модалка | Запуск приложения | Готово | Клиент | Нет | Статика | Да, целиком | — |
| PR-TR-MNEM | «Мнемоника» | — | `js/practice/trainings/mnemonics.js` | — | Заглушка | Хаб «Практика», группа `base` | Заглушка | — | — | — | — | — |
| PR-TR-PREFLOPSTRAT | «Стартовые руки префлопа» | — | `js/practice/trainings/preflop-strategy.js` | — | Заглушка | Хаб «Практика», группа `preflop` | Заглушка | — | — | — | — | — |
| PR-TR-2RIVER | «Вероятность на 2 карты» | — | `js/practice/trainings/two-card-probability.js` | — | Заглушка | Хаб «Практика», группа `flopturn` | Заглушка | — | — | — | — | — |
| PR-TR-OPPHAND | «Рука противника» | — | `js/practice/trainings/opponent-hand.js` | — | Заглушка | Хаб «Практика», группа `river` | Заглушка | — | — | — | — | — |
| LEARNARIUM | Обучариум (Learnarium) | ~1793–1824 (экран-заглушка) | `js/learnarium/learnarium.js` | — | Заглушка «Кампания — пусто». Идея: Ясли/Школа/Университет → уроки (теория+практика) → контрольные, ачивки. **Не пишет свою генерацию раздач — вызывает существующие тренировки через точку входа `PR-ROUTER`** | Вкладка «Обучариум» (уже `Learnarium` в EN-словаре) | Не начато | — | Да | Сервер API + БД (будущее) | — | См. план, Этап 4 |
| STATS | Статистика | 2877–2880 (экран-заглушка) | `js/stats/stats.js` | — | Заглушка «пусто» | Вкладка «Статистика» | Не начато | — | Да | Сервер API + БД (будущее) | — | Зависит от Этапов 2 и 4 |
| UI-CARD | Отрисовка одной карты | (внутри `CORE-EVAL`/`PR-TR-OUTS`, см. примечание) | `js/ui/card.js` | JS | `cardEl` (создаёт DOM-элемент карты), `suitColor` (масть+стиль→цвет), `displayRank` (ранг→текст) — три тесно связанные функции, `cardEl` вызывает две другие | Ауты, Нотация, Комбинации, калькулятор эквити | Готово (физически внутри других диапазонов) | Клиент | Нет | Статика | Да, целиком | Уже фактически общий код, подтверждено 4+ местами вызова. Не разделять на разные файлы — три функции образуют один слой. **Контракт при переносе:** `cardEl(card, faceUp, faceStyle, cardBack)` — стиль колоды и рубашка параметрами, не через `state` напрямую (сейчас `cardEl` читает `state.faceStyle`/`state.cardBack` сама) — см. DECISIONS |
| UI-ANSWEROPTIONS | Список кликабельных вариантов ответа | (внутри `PR-TR-OUTS`, `PR-TR-POSITIONS`, `PR-TR-COMBINATIONS`) | `js/ui/answer-options.js` | JS | Общий паттерн: очистить контейнер → цикл по вариантам → кнопка с обработчиком клика → подсветка состояния | «Ауты» (`createCategoryChip`), «Позиции» (`renderPositionsOptions`), «Комбинации» (`renderComboOptions`) | Готово (дублируется в 3 местах, физически внутри других диапазонов) | Клиент | Нет | Статика | Да, целиком | **Проверено построчно** — `renderComboOptions` и `renderPositionsOptions` структурно почти идентичны, `createCategoryChip` — усложнённый вариант того же паттерна (переключаемый чип с полем ввода). Реальный кандидат на объединение, не гипотеза |
| CORE-SESSION | Прогресс и счёт сессии | (внутри `PR-TR-OUTS`, `PR-TR-POSITIONS`, `PR-TR-COMBINATIONS`) | `js/core/session.js` | JS | Общий паттерн: обновление счётчика «X / N» в DOM; в «Комбинациях» также счётчик правильных/отвеченных (`comboState.correctCount`/`answeredCount`) | «Ауты» (`updateSessionContext`), «Позиции» (`updatePositionsProgress`), «Комбинации» (`updateComboContext` + `comboState.correctCount`/`answeredCount`) | Готово (дублируется в 3 местах, физически внутри других диапазонов) | Клиент | Нет | Статика | Да, целиком | **Проверено построчно** — три функции показа прогресса структурно идентичны. Счётчик правильных ответов пока реализован только в «Комбинациях» вручную, не как общий модуль. **Контракт:** чистый стейт-менеджер без DOM — хранит `currentStep`/`totalSteps`/`correctAnswers`/лог ходов, отдаёт объект состояния; обновление конкретных полей на экране — задача вызывающего `*-ui.js`, не самого `session.js` — см. DECISIONS |

---

## 2. DECISIONS — журнал решений

### Разбиение на файлы — общее
- Переходим от одного `index.html` к структуре файлов по функциональным швам — не тратить токены на весь проект целиком, не терять возможность оценивать результат в совокупности.
- **ES-модули**, не обычные `<script>`-теги. `import` не работает на `file://` — нужен локальный сервер при разработке.

### Финальная структура папок (согласовано, обновлено)
```
pokergym/
├── index.html                       # HTML-SHELL
├── manifest.json                    # Этап 6 — сейчас нет
├── css/
│   └── styles.css                   # CSS-STYLES
├── assets/
│   └── icons/                       # Этап 6 — сейчас в проекте нет ни одной картинки, всё через inline SVG/CSS
└── js/
    ├── app.js                       # APP-BOOT
    ├── router.js                    # ROUTER — переключение вкладок + кнопка «назад» (общее для всего приложения)
    ├── core/
    │   ├── state.js                 # CORE-STATE — чистое хранилище, без импортов из фич-модулей
    │   ├── i18n.js                  # CORE-I18N
    │   ├── deck.js                  # CORE-DECK ⚠ — buildDeck, единственная версия shuffle, случайные утилиты
    │   ├── positions.js             # CORE-POSITIONS ⚠ — posLabel, posTailSeats
    │   ├── hand-eval.js             # CORE-EVAL — evaluate5, evaluateBest (было evaluate7), getCoreCards, displayCategoryIndex, classifyCategories, computeOuts
    │   ├── session.js               # CORE-SESSION — прогресс сессии (X/N) и счёт правильных ответов — единый стандарт, не по одному на тренировку
    │   └── equity-worker.js         # CORE-EQWORKER — модульный воркер, импортирует hand-eval.js по относительному пути './hand-eval.js'; сам URL воркера считается на стороне calc-equity.js через new URL(...)
    ├── ui/                          # все компоненты здесь — «глупые»: отрисовка по входным данным + вызов переданного коллбэка, без своей игровой логики (правило — см. DECISIONS)
    │   ├── card.js                  # UI-CARD — cardEl, suitColor, displayRank; стиль колоды/рубашка параметрами, не через state
    │   ├── card-picker.js           # UI-CARDPICKER
    │   ├── answer-options.js        # UI-ANSWEROPTIONS — рендер вариантов + вызов переданного onSelect, проверку ответа не делает сам
    │   ├── table-seats.js           # UI-TABLESEATS ⚠ — отрисовка стола с местами игроков, принимает список игроков/статусы
    │   └── range-grid.js            # UI-RANGEGRID ⚠ — отрисовка грида 13×13, принимает матрицу состояний + onCellClick
    ├── lib/
    │   └── sortable.js              # LIB-SORTABLE — официальная ESM-сборка
    ├── theory/
    │   ├── theory.js                # THEORY
    │   ├── theory-hub.js            # задел на будущее — сейчас не существует
    │   └── sections/                # задел на будущее — сейчас не существует
    │       ├── terms.js
    │       ├── combos.js
    │       ├── categories.js
    │       └── notation.js
    ├── practice/
    │   ├── practice.js              # PR-HUB
    │   ├── practice-router.js       # PR-ROUTER
    │   └── trainings/
    │       ├── combinations-logic.js   # PR-TR-COMBINATIONS
    │       ├── combinations-ui.js      # PR-TR-COMBINATIONS
    │       ├── positions-logic.js      # PR-TR-POSITIONS
    │       ├── positions-ui.js         # PR-TR-POSITIONS
    │       ├── notation-logic.js       # PR-TR-NOTATION
    │       ├── notation-ui.js          # PR-TR-NOTATION
    │       ├── preflop-grid-logic.js   # PR-TR-PREFLOP-GRID
    │       ├── preflop-grid-ui.js      # PR-TR-PREFLOP-GRID
    │       ├── outs-logic.js           # PR-TR-OUTS
    │       ├── outs-ui.js              # PR-TR-OUTS
    │       ├── calc-outs.js            # PR-TR-CALCOUTS
    │       ├── calc-equity.js          # PR-TR-CALCEQT
    │       ├── mnemonics.js            # PR-TR-MNEM — заглушка
    │       ├── preflop-strategy.js     # PR-TR-PREFLOPSTRAT — заглушка
    │       ├── two-card-probability.js # PR-TR-2RIVER — заглушка
    │       └── opponent-hand.js        # PR-TR-OPPHAND — заглушка
    ├── learnarium/
    │   └── learnarium.js               # LEARNARIUM — заглушка
    ├── stats/
    │   └── stats.js                    # STATS — заглушка
    └── settings/
        └── settings.js                  # SETTINGS
```

⚠ — `core/deck.js`, `core/positions.js`, `ui/table-seats.js`, `ui/range-grid.js` уже упоминаются как переиспользуемый код в примечаниях других строк таблицы раздела 1, но пока не имеют собственной строки там (в отличие от `CORE-SESSION`/`UI-ANSWEROPTIONS`). Добавить строки — на следующей актуализации.

### Принципы структуры папок
- По вкладке — своя папка верхнего уровня (`theory/`, `practice/`, `learnarium/`, `stats/`, `settings/`), названы так же, как вкладки в приложении.
- Файлы называются так же, как тренировки называются в самом приложении, а не по внутренним id.
- Переиспользуемое делится на `core/` (чистая математика/логика, без DOM) и `ui/` (переиспользуемые интерфейсные компоненты).
- `practice/trainings/` — плоская папка, без подпапки на каждую тренировку и без группировки по категориям хаба (та классификация — UI/педагогическая, не структура кода).
- `-logic.js`/`-ui.js` только для тренировок заметно больше ~150–200 строк. **Критерий чистоты:** если в `*-logic.js` встречается `document.getElementById`/`innerHTML` — это сигнал, что код лежит не в том файле.
- `core/state.js` — обязательно чистое хранилище данных, без импортов из фич-модулей — иначе риск циклических зависимостей (например, `state.js` → `settings.js` → `state.js`). Другие модули подписываются на изменения сами (простой Pub/Sub), не наоборот.
- Роутеров — три отдельных, не один: `router.js` (вкладки + «назад», общий), `practice/practice-router.js` (под-экраны Практики), `theory/theory.js` (под-разделы Теории). Раньше это ошибочно считалось одним куском кода в одном диапазоне строк.

### Найденное дублирование и скрытые связи (по факту анализа кода)
- Перемешивание колоды реализовано **трижды**: `shuffle()` (Ауты), `posShuffle()` (Позиции), инлайн-версия в воркере эквити. Объединяются в `core/deck.js`.
- `evaluate5`/`evaluate7`/`displayCategoryIndex` продублированы целиком внутри строки воркера эквити. При переносе — реальный файл, импортирующий `core/hand-eval.js`, все копии удаляются.
- `computeOuts`/`classifyCategories`/`getCoreCards` — уже правильно централизованы и переиспользуются.
- **`evaluate7` на деле универсальна** (работает с 5, 6, 7 и более картами через `combinations(cards, 5)`, не жёстко на 7) — отдельная `evaluate6` не нужна. Название вводит в заблуждение, переименовать в `evaluateBest` при переносе.
- **Тренировка «Матрица префлопа» физически реализована внутри диапазона, который выглядит как «Нотация»** (функции с префиксом `not...`, определяются по заголовку экрана через роутер, не по имени функции) — историческое наследие разработки, не архитектурное решение. Учтено при переносе (`PR-TR-PREFLOP-GRID`).
- **Часть кода из диапазона, который считался общими настройками, на деле относится к сетапу тренировки «Ауты»** (`updateStartButtonState`, `updatePercategoryAvailability` — завязаны на `state.wantNumber`/`wantCategory`, кнопку `start-session`). Перенесено в `PR-TR-OUTS`.
- **Список кликабельных вариантов ответа** (`createCategoryChip` в Аутах, `renderComboOptions` в Комбинациях, `renderPositionsOptions` в Позициях) — проверено построчно, структурно один и тот же паттерн в трёх местах. Кандидат на `ui/answer-options.js`.
- **«Ядро» отрисовки одной карты** (`cardEl`/`suitColor`/`displayRank`) — уже фактически общий код (4+ мест вызова), физически сидит внутри диапазона «Ауты». Выносится в `ui/card.js` без разделения на отдельные файлы — три функции образуют один тесно связанный слой (`cardEl` вызывает две другие), делить их дальше друг от друга не нужно.
- «Стол с местами» (`renderPositionsTable`, без хинтов и проверки ответа) и грид 13×13 (`renderNotationRangeGrid`, без проверки диапазона) — пока используются только в одной тренировке каждый, но это самостоятельные визуальные элементы покера, вероятно понадобятся Обучариуму для показа сценариев/диапазонов без запуска полного интерактива. Выносятся преждевременно, не дожидаясь второго случая использования — дёшево сделать сейчас.
- Правило на будущее: перед написанием `*-logic.js`/`*-ui.js` для новой тренировки — сначала проверять `core/` и `ui/`, не копировать, если похожее уже есть.

### Web Worker и ES-модули
- Нативные Web Workers не понимают `import`/`export` без явного `{ type: 'module' }` при создании. Решение: делать воркер модульным (`new Worker(url, { type: 'module' })`). Альтернатива через `importScripts` рассматривалась, но отклонена в пользу единообразия с остальным ES-модульным кодом. Проверить актуальную поддержку модульных воркеров в целевых браузерах ближе к моменту реализации.
- **Решение по путям импорта исправлено:** не жёсткий абсолютный путь от корня домена (`/js/core/hand-eval.js`) — такой путь сломается, если приложение задеплоено не в корень домена, а в подпапку (например, `username.github.io/pokergym/` на GitHub Pages без кастомного домена — реальный сценарий для Этапа 7). Вместо этого — путь, вычисляемый через `import.meta.url` (относителен фактическому расположению файла воркера, не зависит от глубины подпапки деплоя).
- **Уточнён полный контракт создания воркера (было расписано только для импорта внутри воркера, не хватало второй половины).** Две разные точки, где считается путь, и в каждой — свой способ:
  - **Внутри `core/equity-worker.js`** — импорт `hand-eval.js` по относительному пути: `import { evaluateBest } from './hand-eval.js';`. `equity-worker.js` должен быть полностью автономным файлом, не зависеть от того, откуда его создали.
  - **Внутри `calc-equity.js`** (там, где воркер создаётся) — путь к самому файлу воркера считается через `new URL('../../core/equity-worker.js', import.meta.url)`, и уже этот URL передаётся в `new Worker(url, { type: 'module' })`. `import.meta.url` в этом месте — это URL самого `calc-equity.js`, не воркера, поэтому путь строится относительно него.

### Игровой цикл: прогресс, счёт, обратная связь
- **Подтверждено по коду:** показ прогресса сессии («X / N») продублирован одинаковым паттерном в трёх тренировках (Ауты, Позиции, Комбинации). Счётчик правильных/отвеченных — пока только в «Комбинациях» (`comboState.correctCount`/`answeredCount`), реализован вручную, не как общий модуль.
- Выносится в `core/session.js`. Стандартизированный формат результата хода (успех/неуспех, ответ пользователя, правильный ответ) нужен не только чтобы не копировать код между тренировками, но и потому что Статистике и ачивкам Обучариума (Этапы 4–5) понадобится единый источник данных о ходах — без этого при их реализации пришлось бы либо лезть в код каждой тренировки отдельно, либо получать данные в разном формате от каждой.
- **Согласован формат объекта результата хода** (обычная форма JS-объекта, не TypeScript — в проекте нет TypeScript, `interface` как синтаксическая конструкция в `.js`-файле работать не будет, это была бы ошибка):
  ```js
  // { trainingId: string, isCorrect: boolean, userAnswer: any,
  //   correctAnswer: any, timestamp: number }
  ```
  `trainingId` — совпадает с ID тренировки (`outs`, `positions`, ...), `timestamp` — для будущей аналитики скорости ответов. Каждая `*-ui.js` при обработке ответа передаёт такой объект в `core/session.js`.
- **Важное уточнение контракта `core/session.js` — он не должен трогать DOM.** В монолите подсчёт прогресса жёстко завязан на конкретные DOM-элементы экрана (`#outs-progress` и т.п. по смыслу) — если `session.js` унаследует этот стиль и сам будет искать селекторы, он станет ещё одним UI-модулем и нарушит то же самое правило чистоты, что действует для `core/hand-eval.js` и `core/deck.js`. Контракт: `session.js` — чистый стейт-менеджер (хранит `currentStep`, `totalSteps`, `correctAnswers`, массив логов результатов ходов), при каждом ответе возвращает обновлённое состояние объектом; какие конкретно текстовые поля на экране обновлять — решает и делает вызывающий `*-ui.js`, не `session.js` сам.
- **Не подтверждено по коду, честно как гипотеза:** анимации правильного/неправильного ответа, таймер сессии, «стрейк» (серия подряд правильных ответов) — этого в текущем коде нет ни в одной тренировке. Возможный `ui/game-effects.js` — идея на будущее, не находка дублирования, см. Backlog.

### DTO раздачи для Обучариума
- Обучариум должен уметь передавать тренировке не только настройки (диапазон категорий, число раздач), но и **конкретную заранее заданную раздачу** (эти карты, этот борд) — например, для урока с конкретным разобранным сценарием, не случайной генерацией. Ни одна функция генерации раздачи сейчас (`dealNewHand`, `dealNewPositionsRound`, `dealNewCombo`) не принимает готовую раздачу извне — только генерирует сама.
- Решение: при переносе каждой генерирующей функции в `*-logic.js` на Этапе 1 — сразу закладывать опциональный параметр «готовая раздача» (если передана — использовать её, если нет — генерировать через `core/deck.js`, как сейчас). Дёшево сделать на этапе переноса, дорого — переделывать сигнатуры функций задним числом на Этапе 4.

### Правило «глупых» компонентов в `ui/`
- Компоненты в `ui/` (в первую очередь `table-seats.js`, `range-grid.js`, но правило общее для всех) должны только отрисовывать по входным данным и не слушать клики/не знать о проверке ответа. Обработку взаимодействия ведёт вызывающий `*-ui.js` конкретной тренировки. Иначе компонент перестаёт быть переиспользуемым — он окажется завязан на логику одной конкретной тренировки.
- **Конкретизация для `range-grid.js`:** в монолите клик по ячейке грида (5258–5493) не просто меняет цвет — тут же пересчитывает `pfState.selectedCount`, проверяет лимиты раунда и дёргает валидацию ответа. Если вырезать этот код как есть, компонент окажется намертво спаян с логикой «Матрицы префлопа». Контракт: `range-grid.js` принимает на вход матрицу состояний ячеек (объект/двумерный массив, ключ — код руки типа `AA`/`AKs`) и коллбэк `onCellClick`; сам не хранит и не проверяет ничего игрового. Логика клика, пересчёт `pfState`, проверка ответа — остаются в `preflop-grid-ui.js`, он же передаёт коллбэк и инициирует перерисовку. Без этого Обучариум не сможет использовать грид для статического показа диапазона без запуска интерактива — а это и есть исходная причина, зачем компонент вообще выносится отдельно.
- **Конкретизация для `card.js` (проверено по коду — уточнение важное):** сама `suitColor(suit, faceStyle)` уже чистая функция, `faceStyle` приходит параметром, `state` напрямую не читает. Но `cardEl(card, faceUp)` — читает `state.faceStyle` и `state.cardBack` сама, то есть зависимость от глобального состояния есть на уровень выше, чем казалось. Контракт при переносе: `cardEl(card, faceUp, faceStyle, cardBack)` — стиль колоды и рубашка приходят параметрами от вызывающего `*-ui.js`, `ui/card.js` не импортирует `core/state.js` вообще.

### SortableJS и ES-модули
- **Решение обновлено:** не подключать классическим `<script>`, а взять официальную ESM-сборку библиотеки (`sortable.esm.js` из npm-пакета/CDN), сохранить локально как `js/lib/sortable.js`. Тогда `import Sortable from '../lib/sortable.js'` работает без обращения к глобальной переменной.

### Роутер и точка входа тренировок
- Обучариум не пишет свою генерацию раздач — использует существующие тренировки «Практики» с заданными настройками через параметризуемую точку входа `practice-router.js`.
- Роутер должен инициализировать код модуля при открытии, а не только переключать видимость блоков.
- **Контракт точки входа — асинхронный.** `async function startTraining(moduleId, customSettings = null)`, каждая тренировка экспортирует стандартную `async function init(container, settings)`. Асинхронность не нужна при текущем плане (все тренировки грузятся сразу при старте, гонки быть не может) — но дёшево заложить сразу и снимает вопрос заранее, если/когда дойдём до ленивой подгрузки модулей (`BACKLOG-02`).

### UX: экран настроек тренировки (исходно был пункт в бэклоге, перенесён в решения)
- Направление: тренировка открывается сразу в режиме сессии (настройки по умолчанию/сохранённые), настройки — через шестерёнку и модалку поверх экрана, без отдельного экрана сетапа первым. Технический аргумент: заметно упрощает `practice-router.js` — не нужно плодить пару setup/session под-экранов на каждый модуль, один экран тренировки + одна модалка настроек на всех, единая точка входа вида `startTraining(moduleId, customSettings = null)`. Рекомендация зафиксирована явно (не как нейтральный пересказ вариантов) — окончательное подтверждение перед реализацией всё равно за Романом, поскольку это продуктовое решение.
- **Важное уточнение:** когда `startTraining` вызывается с непустым `customSettings` (то есть тренировку запускает Обучариум с заранее заданными настройками урока, а не пользователь вручную из хаба «Практики») — шестерёнка на экране тренировки не должна отображаться вообще. Пользователь не должен иметь возможность на ходу поменять параметры конкретного заданного сценария урока. Это требование к контракту `startTraining` и к `practice-router.js`, закладывается сразу при разработке роутера, не задним числом на Этапе 4.

### Работа с проектом в чате (без Claude Code)
- Project Knowledge (Документы проекта) — плохая идея для активно редактируемого многофайлового проекта: нет папок, а ниже ~200К токенов Документы грузятся в контекст целиком при каждом сообщении.
- `ARCHITECTURE.md` — небольшой ориентир в Документах (грузится автоматически); код — точечно через `web_fetch` по ссылкам на GitHub, по одному файлу за задачу. Требует публичного репозитория.
- Отдельный Project для PokerGym — своя изолированная память и поиск.

### Claude Code
- Работает напрямую с файлами на диске, `CLAUDE.md` в корне читается автоматически. Начинать можно в любой момент. Доступен из терминала, десктоп-приложения, браузера («Claude Code on the web» — отдельная облачная песочница), мобильного приложения. Рекомендуется изоляция при автономном режиме — использовать простаивающую VM с Ubuntu в VirtualBox. Сессия не видит переписку в обычном чате — план копировать явно, лучше сразу в `CLAUDE.md`. Пока не переходим.

### PWA
- PWA = сайт + `manifest.json` + service worker. Сейчас ни того, ни другого нет. Известная болячка — обновление закэшированной версии без продуманного версионирования. Поддержка PWA у Apple менее стабильна, чем у Google/Android.

### Деплой
- Деплой происходит по `git push`, не по каждому визиту. Публичность репозитория и публичность сайта — независимые вещи. Минификация — часть сборки, которой сейчас нет в проекте. Файлы репозитория при деплое остаются нетронутыми.

### Приватность репозитория
- Публичный репозиторий, пока в нём только клиентская логика — риск низкий. Автоматические боты сканируют все публичные репозитории на предмет секретов — секреты не коммитить никогда. Закрыть репозиторий — когда появится код авторизации/оплаты.

### Целевая архитектура ресурсов
- Три слоя: хостинг статики → сервер API → база данных. Начинать с управляемых отдельных сервисов, не с одного своего сервера.

### localStorage
- Формат — JSON. Чисто локальные предпочтения (язык, тема, порядок хаба) — только `localStorage`. Данные аккаунта (подписка, прогресс, статистика) — источник правды сервер/БД, `localStorage` — кэш для офлайн/скорости, синхронизация при подключении. Сейчас `localStorage` не существует вообще.

### Защита
- Обязательная (пароли/HTTPS/токены/платёжный провайдер) — стандартные средства. Защита от копирования — принципиально не стопроцентная. Стратегия: «сделать бесполезным без сервера», не «сделать невозможным» — ценная логика (генератор Обучариума) переносится на сервер. Конфликт целей: офлайн-режим vs частая ревалидация лицензии — компромисс, ограниченный офлайн-срок. Простая мера сейчас — лимит одновременных сессий. Водяные знаки — отложено до признаков реальной проблемы. Юридическая мера — пользовательское соглашение. Общая калибровка: серьёзная защита кода — практика крупных игроков в конкурентных нишах, для небольшого продукта — дешёвый минимум сейчас, дорогие меры по факту необходимости.

### App Store / Google Play
- Android — TWA, Bubblewrap/PWABuilder, 80+ баллов Lighthouse, 25$ разово. iOS — менее стабильная поддержка PWA у Apple, реалистично нативная обёртка с WebView, 99$/год, решение отложено.

### Хранение пользовательских данных
- Внутренние данные приложения (статика) vs внешние данные пользователей (сервер+БД) — разная инфраструктура. Пароли — только хешированные. Открытый вопрос: требования 152-ФЗ о хранении персональных данных в РФ — проверить отдельно, не юридическая консультация.

---

## 3. План игры

### Основная линия

**Этап 1 — Разбиение на файлы**
- [ ] Настроить локальный сервер для разработки
- [ ] Решить: экран настроек тренировки — шестерёнка+модалка или отдельный сетап-экран (до проектирования точки входа роутера); при запуске из Обучариума с заданными настройками — шестерёнка не показывается
- [ ] Вынести CSS
- [ ] Подключить ESM-сборку SortableJS
- [ ] Вынести весь `core/` (state, i18n, deck — с объединением трёх копий shuffle, positions, hand-eval — с переименованием evaluate7→evaluateBest, session — прогресс+счёт, equity-worker — модульным воркером, путь к hand-eval.js через `import.meta.url`)
- [ ] Вынести весь `ui/` (card, card-picker, answer-options, table-seats, range-grid — последние два строго «глупыми», без обработки кликов)
- [ ] Разнести тренировки по `practice/trainings/`, включая перенос `updateStartButtonState`/`updatePercategoryAvailability` в `outs-logic.js`, функций «матрицы префлопа» из диапазона «нотации», и добавление опционального параметра «готовая раздача» в каждую функцию генерации (задел под Обучариум)
- [ ] Вынести `practice.js`, `practice-router.js`, `theory.js`, `settings.js`, `router.js` (с чётким разделением: вкладки/назад — в `router.js`, под-экраны — в `practice-router.js`)
- [ ] Доработать `router.js`/`practice-router.js`: инициализация модуля при открытии + точка входа для Обучариума
- [ ] Уточнить orphaned-ключи словаря (`identifyCategories`, `handprob`)
- [ ] Проверить приложение целиком, запушить, проверить `web_fetch`
- [ ] Обновить таблицу в разделе 1

**Этап 2 — Локальное сохранение состояния**
- [ ] Добавить `localStorage`, разделить данные на локальные и кандидаты на синхронизацию с сервером

**Этап 3 — Минимальный рабочий пул тренировок в новой структуре**
- [ ] Прогнать все тренировки после разбиения, обновить таблицу

**Этап 4 — Обучариум (Learnarium)**
- [ ] Модель данных: Ясли / Школа / Университет
- [ ] Уроки (теория+практика), контрольные/экзамены
- [ ] Использовать существующие тренировки через `practice-router.js`
- [ ] Ачивки, экран вместо заглушки

**Этап 5 — Статистика**
- [ ] Метрики, экран вместо заглушки, связь с ачивками

**Этап 6 — PWA-обёртка**
- [ ] `manifest.json`, иконки, service worker, стратегия обновления кэша, проверка установки

**Этап 7 — Публикация бесплатной версии**
- [ ] Хостинг, деплой, домен

**Этап 8 — Серверная часть**
- [ ] Сервер API, БД, авторизация, платёжный провайдер, приём платежей из РФ, проверка 152-ФЗ

**Этап 9 — Защита**
- [ ] Перенос ценной логики на сервер, лицензионный токен, лимит сессий, пользовательское соглашение, (отложено) водяные знаки

**Этап 10 — Магазины приложений**
- [ ] Android (TWA), iOS отдельно и позже

### Параллельные треки
- Доработка заглушек тренировок
- Дизайн/интерфейс, шапка
- Юридическое (соглашение, политика конфиденциальности, 152-ФЗ)
- Мониторинг ошибок, аналитика, резервное копирование, стейджинг
- Поддержка пользователей

### Backlog
- **BACKLOG-01.** Порядок карточек хаба и закрепление живут только в памяти — связано с Этапом 2.
- **BACKLOG-02.** Ленивая подгрузка модулей (code splitting) — потенциальная оптимизация, не критично.
- **BACKLOG-03.** Осиротевшие ключи словаря (`identifyCategories`, `handprob`) — уточнить, заготовка или мусор.
- **BACKLOG-04.** Общая кнопка «назад» сейчас напрямую знает детали и Практики, и Теории — кандидат на рефакторинг (каждая вкладка отдаёт свой `goBack()`), не критично для Этапа 1.
- **BACKLOG-05.** `ui/game-effects.js` — стандартизированные анимации правильного/неправильного ответа, таймер хода, шапка сессии. Идея на будущее, не подтверждена по текущему коду (сейчас такого нет ни в одной тренировке) — оценить при доработке заглушек тренировок.
