# PokerGym — ARCHITECTURE.md

**Версия: v16**

Живой документ: таблица компонентов, структура файлов, план. Технические и продуктовые решения, контракты, найденные при разборе особенности — в `DECISIONS.md`.

**Таблица ниже отражает реальные текущие файлы** — пути, статусы и объём соответствуют тому, что сейчас лежит в репозитории, не старому монолиту и не плану.

Содержание:
1. [Таблица компонентов](#1-таблица-компонентов)
2. [Структура файлов](#2-структура-файлов)
3. [План игры](#3-план-игры)

---

## 1. Таблица компонентов

**Как читать столбцы:**

| Столбец | Смысл |
|---|---|
| ID | Короткий код, отражает путь файла |
| Кусок / модуль | Человеческое название |
| Строк | Реальный объём текущего файла (число строк) — ориентир для порога `-logic`/`-ui` (~150–200 строк, см. `DECISIONS.md`) |
| Путь | Реальный текущий путь файла |
| Технология | HTML/CSS/JS |
| Что делает | Подробное описание |
| Где используется | Вкладка / тренировка (реальный id из `HUB_MODULES`, если отличается от имени файла) |
| Статус | готово / заглушка |
| Подключён | да / нет — реально загружается и работает в `index.html` прямо сейчас |
| Кандидат на сервер | да / нет / частично |
| Примечания | что важно знать именно про этот файл; за «почему так» — в `DECISIONS.md` |

| ID | Кусок / модуль | Строк | Путь | Технология | Что делает | Где используется | Статус | Подключён | Кандидат на сервер | Примечания |
|---|---|---|---|---|---|---|---|---|---|---|
| CSS-STYLES | Все стили | 1847 | `css/styles.css` | CSS | Визуальная стилистика: экраны, карточки, кнопки, темы, модалки | Везде | Готово | Да | Нет | — |
| HTML-SHELL | Разметка экранов и модалок | 1374 | `index.html` | HTML | Каркас: шапка, модалки настроек всех пяти сессионных тренировок (Ок/Отмена, включая блок «Скрывать карты после показа» в шестерёнках «Аутов» и «Комбинаций»), модалка «i»+обратная связь, заглушка входа, статичные части (теория, настройки, расчётный статус-бар показа карт) | Везде | Готово | Да | Нет | Один `<script type="module" src="js/app.js">`, ни одного inline `<style>`/`<script>`. Подробности контракта шапки/модалок — в `DECISIONS.md` |
| LIB-SORTABLE | SortableJS 1.15.7 | 3363 | `js/lib/sortable.js` | JS (сторонняя, официальная ESM-сборка из npm) | Drag-and-drop карточек в хабе «Практика» | `practice.js`, режим редактирования | Готово | Да | Нет | — |
| CORE-STATE | Глобальное состояние и константы | 49 | `js/core/state.js` | JS | Объект `state`, цвета тем (`tableBgs`), константы карт (`RANKS`, `SUITS`, `RANK_VALUE`, `DRAW_TYPES`, `OTHER_OUT_TYPES`). `state.hideCards`/`state.showTime` — фактически «свои» для «Аутов» (см. DECISIONS.md, контракт «Показ карт») | Везде | Готово | Да | Нет | Чистое хранилище, без импортов из фич-модулей |
| CORE-I18N | Словарь локализации RU/EN | 820 | `js/core/i18n.js` | JS | Все текстовые строки интерфейса | Везде | Готово | Да | Нет | Ключ `settings.timeNone` удалён вместе с опцией «Без ограничения» (см. DECISIONS.md). Добавлены `settings.cardDisplayStatusSame`/`...Different` |
| CORE-DECK | Колода, перемешивание, случайные утилиты | 54 | `js/core/deck.js` | JS | `buildDeck`, `shuffle` (полная), `partialShuffle` (частичная, для воркера), `randomInt`, `pickRandom` | Движок, «Ауты», «Позиции», «Нотация», воркер эквити | Готово | Да | Да (движок от него зависит) | `randomInt` проверен на равномерность (200k прогонов) — генератор числа игроков в «Позициях» не смещён |
| CORE-POSITIONS | Определение и отображение позиции за столом | 49 | `js/core/positions.js` | JS | `posLabel`, `posTailSeats` + константы `POS_SHORT_LABEL`/`POS_FULL_KEY` | «Позиции», кандидат на переиспользование в Обучариуме | Готово | Да | Нет | — |
| CORE-EVAL | Движок оценки руки и комбинаторика | 261 | `js/core/hand-eval.js` | JS | `evaluate5`, `evaluateBest`, `getCoreCards`, `displayCategoryIndex`, `classifyCategories`, `computeOuts`, `cardId` | «Ауты», «Комбинации», оба калькулятора | Готово | Да | Да (стратегия защиты) | **Сверено с pokersolver — не менять без веской причины** |
| CORE-SESSION | Прогресс и счёт сессии | 55 | `js/core/session.js` | JS | `createSession`/`recordAnswer`/`isSessionComplete` — чистый стейт-менеджер, формат `ActionFeedback` | Спроектирован для будущей Статистики (Этап 5) | Готово | **Нет** | Нет | Тренировки по-прежнему считают счёт по-старому, каждая сама — сознательно, см. `DECISIONS.md` |
| CORE-STORAGE | Настройки в `localStorage` | 48 | `js/core/storage.js` | JS | `loadSection`/`saveSection` — версия схемы внутри данных, безопасная деградация при недоступности хранилища | Все места с настройками (Этап 2) | Готово | Да | Нет | Хранит только настройки/предпочтения, не прогресс — см. `DECISIONS.md` |
| CORE-EQWORKER | Web Worker для расчёта эквити | 88 | `js/core/equity-worker.js` | JS (модульный Worker) | Монте-Карло расчёт эквити рук друг против друга | Калькулятор эквити | Готово | Да | Да | Путь к самому воркеру — `new URL(...)` от `calc-equity.js`, не жёсткий |
| UI-CARD | Отрисовка одной карты | 35 | `js/ui/card.js` | JS | `cardEl`, `suitColor`, `displayRank` — «глупый» компонент | Ауты, Нотация, Комбинации, калькулятор эквити | Готово | Да | Нет | Стиль колоды/рубашка параметрами, не через `state` |
| UI-CARDPICKER | Модалка выбора карты | 112 | `js/ui/card-picker.js` | JS | `openCardPicker`, `renderSeqCardRow` | Оба калькулятора | Готово | Да | Нет | Читает `state.faceStyle`/`cardBack` напрямую — сознательно, не как `card.js` |
| ROUTER | Переключение вкладок + кнопка «назад» | 59 | `js/router.js` | JS | Общая навигация верхнего уровня. Останавливает таймеры обеих тренировок с ревилом (`stopTimer`, `stopComboTimer`) при уходе с экрана; при заходе на вкладку «Настройки» пересчитывает расчётный статус-бар показа карт | Всё приложение | Готово | Да | Нет | — |
| PR-HUB | Хаб «Практика» | 278 | `js/practice/practice.js` | JS | `HUB_MODULES`, рендер карточек, закрепление, drag, режим редактирования | Вкладка «Практика» | Готово | Да | Нет | Порядок карточек и закрепление — только в памяти, сброс при перезагрузке (Этап 2) |
| PR-ROUTER | Навигация по под-экранам «Практики» | 74 | `js/practice/practice-router.js` | JS | `showSubview`, реестр `onSubviewShow`/`onAnySubviewShow`, `startTraining(moduleId, customSettings)` + `registerTrainingEntry` | Вкладка «Практика», все тренировки, Обучариум (когда появится) | Готово | Да | Нет | Точка входа для Обучариума построена и проверена |
| THEORY | Вкладка «Теория» | 64 | `js/theory/theory.js` | JS | `showTheorySubview` + поиск по глоссарию | Вкладка «Теория» | Готово | Да | Нет | Сам текст глоссария статичен, в `HTML-SHELL` |
| SETTINGS | Общие настройки и тема | 257 | `js/settings/settings.js` | JS | Язык, тема, стиль/рубашка карт. Блок «Показ карт» больше не хранит своё значение — это «применить обеим тренировкам сразу» + расчётный статус-бар (`refreshCardDisplayStatus`, экспортирована для router.js) | Вкладка «Настройки» | Готово | Да | Нет | `applyLanguage` — самая связанная функция, дёргает узкие экспорты из пяти других файлов |
| PR-TR-OUTS | Тренировка «Ауты и вероятность» | 632 | `js/practice/trainings/outs.js` | JS | Генерация раздачи, таймер, приём и разбор ответа, сетап. `hideCards`/`showTime` — теперь собственная настройка тренировки (была в `global`), контракт снимок+Ок/Отмена как у остальных полей. При проверке ответа принудительно раскрывает руку и глушит таймер, если она ещё скрыта | id хаба: `outs` | Готово | Да | Частично | ⚠ заметно больше порога `-logic`/`-ui` (150–200), см. `BACKLOG-06` — актуальность записи стоит переоценить, файл продолжает расти |
| PR-TR-NOTATION | Тренировка «Нотация» | 459 | `js/practice/trainings/notation.js` | JS | Три режима заявки руки | id хаба: `notation` | Готово | Да | Нет | ⚠ см. `BACKLOG-06` |
| PR-TR-PREFLOP-GRID | Тренировка «Матрица префлопа» | 385 | `js/practice/trainings/preflop-grid.js` | JS | Генерация раунда с диапазоном, грид 13×13, вопрос на вероятность | **id хаба: `preflop-table`**, не `preflop-grid` | Готово | Да | Нет | ⚠ см. `BACKLOG-06` |
| PR-TR-POSITIONS | Тренировка «Позиции за столом» | 406 | `js/practice/trainings/positions.js` | JS | Генерация раздачи по позициям, подсказки | id хаба: `positions` | Готово | Да | Нет | ⚠ см. `BACKLOG-06`. `randomInt(minN, maxN)` проверен экспериментально на равномерность — жалоба на «нечестное» число игроков не подтвердилась кодом, статистический шум малой выборки (см. чат) |
| PR-TR-COMBINATIONS | Тренировка «Комбинации» | 391 | `js/practice/trainings/combinations.js` | JS | Генерация руки нужной сложности, проверка, скрытие руки после показа (борд всегда виден) — реализовано с нуля по образцу «Аутов», для подуровня «5 карт» неприменимо (нет деления рука/борд), блок в шестерёнке дизейблится | **id хаба: `identify-combo`**, не `combinations` | Готово | Да | Нет | ⚠ объём почти удвоился с прошлой версии документа (214→391) из-за ревил-логики, см. `BACKLOG-06` |
| PR-TR-CALCOUTS | Калькулятор аутов | 139 | `js/practice/trainings/calc-outs.js` | JS | Ручной ввод руки/борда, расчёт через `CORE-EVAL` | id хаба: `calc-outs` | Готово | Да | Да | Собственное состояние (`calcCurrentDeal`), не делит с `outs.js` |
| PR-TR-CALCEQT | Калькулятор эквити — интерфейс | 191 | `js/practice/trainings/calc-equity.js` | JS | UI: до 9 игроков, ввод рук/борда, прогресс-бар | id хаба: `calc-equity` | Готово | Да | Да | Сам расчёт — в `CORE-EQWORKER` |
| PR-TR-MNEM | «Мнемоника» | 4 | `js/practice/trainings/mnemonics.js` | — | Заглушка | id хаба: `mnemonic` | Заглушка | Да | — | Контент не продуман |
| PR-TR-PREFLOPSTRAT | «Стартовые руки префлопа» | 4 | `js/practice/trainings/preflop-strategy.js` | — | Заглушка | id хаба: `preflop` | Заглушка | Да | — | — |
| PR-TR-2RIVER | «Вероятность на 2 карты» | 4 | `js/practice/trainings/two-card-probability.js` | — | Заглушка | id хаба: `tworiver` | Заглушка | Да | — | — |
| PR-TR-OPPHAND | «Рука противника» | 4 | `js/practice/trainings/opponent-hand.js` | — | Заглушка | id хаба: `opphand` | Заглушка | Да | — | — |
| LEARNARIUM | Обучариум (Learnarium) | 5 | `js/learnarium/learnarium.js` | — | Заглушка | Вкладка «Обучариум» | Заглушка | Да | Да (будущее) | Не пишет свою генерацию — вызывает тренировки через `PR-ROUTER`. Точка входа `startTraining(moduleId, customSettings)` теперь умеет и `hideCards`/`showTime` в `customSettings` для «Аутов»/«Комбинаций» — ровно то, ради чего затевался перенос настройки показа карт из `global` |
| STATS | Статистика | 3 | `js/stats/stats.js` | — | Заглушка | Вкладка «Статистика» | Заглушка | Да | Да (будущее) | Зависит от Этапов 2 и 4 |
| APP-BOOT | Точка входа | 34 | `js/app.js` | JS | Импорт всех модулей ради побочных эффектов + вступительная модалка | Запуск приложения | Готово | Да | Нет | — |

## 2. Структура файлов

Без изменений с прошлой версии — новых файлов не добавлялось, только правки внутри существующих (см. таблицу выше и `DECISIONS.md`).

## 3. План игры

### Основная линия

**Этап 1 — Разбиение на файлы — ЗАКРЫТ**
**Этап 2 — Локальное сохранение состояния — ЗАКРЫТ**

Подробности по обоим — без изменений, см. предыдущие версии документа в истории git.

**Этап 3 — Наполнение: тренировок и материала «Теории» пока не хватает для Обучариума**
- [x] Прогнать все существующие тренировки после разбиения — сделано, все рабочие
- [x] Скрытие карт после показа — было сломано («Ауты»), теперь не просто починено, а перестроено: своя настройка на каждую тренировку, добавлено в «Комбинации» (раньше не было вообще), убрана избыточная опция «Без ограничения» (см. `DECISIONS.md`)
- [ ] Доработать тренировку «Мнемоника»
- [ ] Доработать тренировку «Стартовые руки префлопа»
- [ ] Доработать тренировку «Вероятность на 2 карты»
- [ ] Доработать тренировку «Рука соперника»
- [ ] Расширить содержание вкладки «Теория» — сейчас, вероятно, недостаточно материала под уроки Обучариума

**Этап 4 — Обучариум (Learnarium)**
- [ ] Модель данных: Ясли / Школа / Университет
- [ ] Уроки (теория+практика), контрольные/экзамены
- [ ] Использовать существующие тренировки через `practice-router.js`
- [ ] Ачивки, экран вместо заглушки

**Этап 5 — Статистика**
- [ ] Метрики, экран вместо заглушки, связь с ачивками

**Этап 6 — PWA-обёртка**
- [ ] `manifest.json`, иконки, service worker, стратегия обновления кэша, проверка установки
- Порядок этапов (PWA — после наполнения, не раньше) подтверждён сознательно: обвязка добавляет обязательную дисциплину на каждый деплой (версия кэша), дешевле внедрять её, когда фичи устаканятся и деплои станут реже — см. `DECISIONS.md`

**Этап 7 — Публикация бесплатной версии**
- [ ] Хостинг, деплой, домен

**Этап 8 — Серверная часть**
- [ ] Сервер API, БД, авторизация, платёжный провайдер, приём платежей из РФ, проверка 152-ФЗ

**Этап 9 — Защита**
- [ ] Перенос ценной логики на сервер, лицензионный токен, лимит сессий, пользовательское соглашение, (отложено) водяные знаки

**Этап 10 — Магазины приложений**
- [ ] Android (TWA), iOS отдельно и позже

### Параллельные треки
- Дизайн/интерфейс, шапка
- Юридическое (соглашение, политика конфиденциальности, 152-ФЗ)
- Мониторинг ошибок, аналитика, резервное копирование, стейджинг
- Поддержка пользователей

### Backlog
- **BACKLOG-01.** Порядок карточек хаба и закрепление живут только в памяти — связано с Этапом 2.
- **BACKLOG-02.** Ленивая подгрузка модулей (code splitting) — потенциальная оптимизация, не критично.
- **BACKLOG-03.** ~~Осиротевшие ключи словаря~~ — сделано, удалены (`identifyCategories`, `handprob`, и теперь `settings.timeNone`).
- **BACKLOG-04.** Общая кнопка «назад» сейчас напрямую знает детали и Практики, и Теории — кандидат на рефакторинг (каждая вкладка отдаёт свой `goBack()`), не критично для Этапа 1.
- **BACKLOG-05.** `ui/game-effects.js` — стандартизированные анимации правильного/неправильного ответа, таймер хода, шапка сессии. Идея на будущее — теперь частично пересекается с уже написанной таймер-логикой показа карт в `outs.js`/`combinations.js` (`timer-bar-track`/`timer-bar-fill`, переиспользуется как есть, не абстрагирована). Оценить при доработке заглушек тренировок.
- **BACKLOG-06.** Разделение каждой тренировки на `-logic.js`/`-ui.js` — не делаем сейчас, каждая тренировка остаётся одним файлом. `outs.js` (632 строки) и `combinations.js` (391, почти удвоился за счёт ревил-логики) заметно дальше от порога ~150–200, чем на момент, когда это правило писалось — актуальность формулировки стоит пересмотреть в следующий содержательный заход в эти файлы, не сейчас.
- ~~**BACKLOG-07.** Шестерёнка+модалка вместо экрана сетапа~~ — **сделано.**

### Закрытые вопросы (были «не потерять при переходе в новый чат»)
- ~~«Скрывать карты после показа» не работает~~ — **решено.** Причина была не в CSS/анимации (гипотеза о проблеме, не видимой через `jsdom`, не подтвердилась) — баг был не там, где искали изначально. Заодно фича перестроена: своя настройка на каждую тренировку вместо общей, добавлена в «Комбинации» с нуля, убран мёртвый код и избыточная опция. Подробный контракт — в `DECISIONS.md`.
